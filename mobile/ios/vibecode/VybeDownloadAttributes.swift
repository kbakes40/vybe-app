import Foundation
import ActivityKit

/// Legacy name — same type as `VybeActivityAttributes` (single shared definition).
@available(iOS 16.1, *)
public typealias VybeDownloadAttributes = VybeActivityAttributes
