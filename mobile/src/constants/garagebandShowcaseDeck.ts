import type { Track } from '@/types/music';

/**
 * Optional extra tiles for the Home “Your vinyls” row (no mock / demo tracks).
 * Populate from a real API or curated remote list when you add one.
 * Resolved `GARAGEBAND_VINYLS` assets in `garagebandLibrary.ts` always list first on Home.
 */
export const GARAGEBAND_SHOWCASE_DECK: Track[] = [];
