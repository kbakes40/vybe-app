/**
 * Expanded Dynamic Island mini-controller — height bounds (see {@link DynamicIsland}).
 * Tab top inset uses {@link EXPANDED_PILL_MAX_HEIGHT_PT} so lists clear worst-case wrap.
 */
/** Original committed pill height (pre-growth). Min == Max so the pill never expands beyond this. */
export const EXPANDED_PILL_MIN_HEIGHT_PT = 112;
export const EXPANDED_PILL_MAX_HEIGHT_PT = 112;

/** Extra vertical space when the title uses a second line (kept at 0 — title is single-line again). */
export const EXPANDED_EXTRA_TITLE_LINE_PT = 0;
