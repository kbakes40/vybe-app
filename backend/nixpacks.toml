[phases.setup]
aptPkgs = ["python3", "python3-pip", "ffmpeg", "libssl3", "openssl", "ca-certificates"]
nixPkgs = ["openssl"]

[phases.install]
cmds = ["pip3 install --break-system-packages yt-dlp", "bun install"]

[phases.build]
cmds = ["yt-dlp --version"]

[start]
cmd = "bun run start"
