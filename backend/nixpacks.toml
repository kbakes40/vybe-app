[phases.setup]
aptPkgs = ["python3", "python3-pip", "ffmpeg", "openssl", "ca-certificates"]

[phases.install]
cmds = ["pip3 install --break-system-packages yt-dlp", "bun install"]

[phases.build]
cmds = ["yt-dlp --version"]

[start]
cmd = "bun run start"
