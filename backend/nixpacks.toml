[phases.setup]
aptPkgs = ["python3", "python3-pip", "ffmpeg"]
nixPkgs = ["yt-dlp"]
nixLibs = ["openssl"]

[phases.install]
cmds = ["pip3 install --break-system-packages yt-dlp || true", "bun install"]

[phases.build]
cmds = ["yt-dlp --version"]

[start]
cmd = "bun run start"
